# Security

Please do not open public issues containing private workspace IDs, job names, table names, tokens, or billing data.

Report sensitive issues through GitHub private vulnerability reporting if available, or contact the maintainer through the repository profile.

`dbx-run-cost-py` reads billing metadata and can write cost rows to a table. Review permissions before using it in production.
